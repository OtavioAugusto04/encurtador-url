network:
  version: 2
  ethernets:
    enp0s3:
      dhcp4: true
      dhcp6: false
      dhcp4-overrides:
        use-routes: false
        use-dns: false
      dhcp6-overrides:
        use-routes: false
        use-dns: false
    enp0s8:
      addresses: [10.20.30.2/24]
      dhcp4: no
      routes:
        - to: default
          via: 10.20.30.1
      nameservers:
        addresses: [8.8.8.8]